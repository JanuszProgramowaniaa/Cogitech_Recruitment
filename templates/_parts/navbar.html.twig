<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_index') }}">Wasiak Damian</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                {% if not app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Zaloguj się</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Zarejestruj się</a>
                    </li>
                {% endif %}
            </ul>
        </div>

        {% if app.user %}
            <div class="ms-auto">
                <div class="d-flex justify-content-end align-items-center">
                    <p class="mb-0 me-2 text-light">Jesteś zalogowany jako {{ app.user.userIdentifier }},</p>
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-primary btn-sm">Wyloguj</a>
                </div>
            </div>
        {% endif %}
    </div>
</nav>